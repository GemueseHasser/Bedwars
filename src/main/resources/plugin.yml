name: '${project.name}'
description: '${project.description}'
version: '${project.version}'
load: 'POSTWORLD'
author: 'Jonas0206'
authors: ['Jonas0206']
website: '${project.url}'
api-version: '1.8.8'
main: '${project.groupId}.${project.artifactId}.${project.name}'
depend: []
softdepend: []
loadbefore: []
prefix: '${project.name}'

commands:
  setspawner:
    description: 'Setzte den jeweiligen Spawner'
    usage: '</command> <bronze|eisen|gold>'