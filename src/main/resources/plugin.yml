name: '${project.name}'
description: '${project.description}'
version: '${project.version}'
load: 'POSTWORLD'
author: 'Jonas0206'
authors: ['Jonas0206']
website: '${project.url}'
api-version: '1.8.8'
main: '${project.groupId}.${project.artifactId}.${project.name}'
depend: []
softdepend: []
loadbefore: []
prefix: '${project.name}'

commands:
  setspawner:
    description: 'Setzt den jeweiligen Spawner'
    usage: '</command> <bronze|eisen|gold>'
  setshop:
    description: 'Setzt den Bedwars-Shop'
    usage: '</command>'
  setspawn:
    description: 'Setzt den jeweiligen Spieler-Spawn'
    usage: '</command> <blau|rot>'
  setbed:
    description: 'Setzt das Bett'
    usage: '</command> <blau|rot>'
  setwaitinglobby:
    description: 'Setzt die warte-lobby!'
    usage: '</command>'
  setlobby:
    description: 'Setzt die lobby!'
      usage: '</command>'